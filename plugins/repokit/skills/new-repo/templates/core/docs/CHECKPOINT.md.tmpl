# Checkpoint — {{name}}

Resume state: what a fresh session needs to pick this repo up. Keep every line current — the
pre-commit checklist has the tripwire, and stale entries are worse than none.

- Last updated: {{today}}
- Status: PLANNED
- In progress: nothing
- Next step: review the scaffold, then start the first real task

One line per fact, current state only — history lives in `CHANGELOG.md` and git. If this repo
adopts the living-docs add-on, `docs/STATE.json` can take over as the resume-state artifact;
declare the substitution in the START-HERE map.
