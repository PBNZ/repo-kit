# skill-plugin type overlay (stub)

Not built yet. Scaffolding a repo of this type stamps the Core / Public / Published tier files,
but no type-specific structure. Fill this overlay in when you first need it, following the pattern
in `types/powershell-module/` (tiered as `core/`, `public/`, `published/`) and the standard at
`plugins/repokit/skills/repo-standard/standard/the-standard.md`. Add a row to the testing matrix
when you do.
